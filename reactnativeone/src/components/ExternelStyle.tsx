import { View, Text } from 'react-native'
import React from 'react'
import externerStyle from '../utils/style'

const ExternelStyle = () => {
  return (
    <View>
      <Text style={externerStyle.exStyle}>ExternelStyle</Text>
    </View>
  )
}

export default ExternelStyle